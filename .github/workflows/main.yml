name: Build and publish dockee image

on:
  push:
    branches: [ "dev" ]
jobs:
  build_and_push_docker_to_rep:
    name: push docker image to registry
    runs-on: ubuntu-latest
    steps:
    - name: checkout repo
